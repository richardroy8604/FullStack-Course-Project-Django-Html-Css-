# Git Workflow Preference

- When the user says "commit", stage (`git add .`) and make a local commit (`git commit -m "..."`). Do NOT push to remote.
- When the user says "push", push the local commits to the remote repository (`git push origin main`).
